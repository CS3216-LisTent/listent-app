import bufferToWave from "audiobuffer-to-wav";

const audioContext = new (window.AudioContext || window.webkitAudioContext)()


export default function convertToChipmunk(
  audioBlob,
  setAudioSrc,
  setRecordedBlob,
  setConversionProgress
) {}
